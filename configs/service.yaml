# ===========================================================================
#  서비스 설정 (SMILES → descriptor 자동 계산 → 추론)
# ===========================================================================

service:
  checkpoint_path: "checkpoints/best_model.pt"
  batch_size: 64
  output_path: "outputs/service_predictions.csv"
